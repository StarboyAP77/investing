# STATE.md

- Position: Phase 1 human-verified. Phase 2 committed on confidence-delegation (explicit verify still open). Phase 3 committed. Phase 4 executed, awaiting human verify. Phases 5–6 not started.
- Phase 4 deltas: `index.html` 6604→1433 lines (shell: markup + 3 CSS links + `js/main.js`); `css/` (base/print/components incl. quarterly) + `js/` (state/metrics/engine/benchmark/charts-core/tables/quarterly/crash/valuation/deployment/simulate/main, ES modules, DAG imports); worker script stays inline; `buildMergedData(rawJsonData, strategy)` now pure. Verified: `node --check` ×12 OK, ESM harness with real imports green (identical 5025.12%/20.53%, shift/idempotent/null), all 18 files serve 200 with correct MIME, 2 spread-missed imports caught+fixed by harness.
- Phase 3 deltas: new `renderVisualizations(calc)` (~L5198, byte-exact render move, existing ECharts `setOption` only); `updateChartAndTable()` now visibility + `calculateMetrics(data)` + `renderVisualizations(calc)`; fixed 3-instance leak in `updateDeploymentBattle` (`dep-wealth/cost/gauge` → `getInstanceByDom || init`); `#strategy-selector` excluded from generic `input` listener (was double-rendering). Verified: `node --check` OK, braces 1796/1796, zero bare `echarts.init`, zero setOption in delegator, no absolute paths, harness calc regression identical (5025.12%/20.53%), 3 spread-hidden imports (`...getBaseChartOptions()`) caught+fixed in tables/quarterly/main after user reported browser error; hardened harness now runs FULL init render with zero errors.
- Phase 2 deltas: new `calculateMetrics(mergedData)` (~L5031, closure-scoped, `window.calculateMetrics` hook) — purge + input snapshot + date filter on param + 4-strategy backtests + headline/rolling/with-withdrawal metrics + globals store, returns 14-field bundle. `updateChartAndTable()` now delegates (`calculateMetrics(data)` → destructure → render-only). Verified live in Node+DOM-stub harness on real data.json: reb Abs 5025.12%/CAGR 20.53% vs gold-as-equity 1764.81%/14.89% (shift YES), repeat identical (no contamination), short→null. `node --check` OK, braces balanced.
- Phase 1 deltas: baked array deleted by user → `let data=[]`; added `#strategy-selector` (~L581), `STRATEGY_KEYS`/`rawJsonData`/`currentStrategy`/`buildMergedData()` (~L2016), async fetch + selector wiring (~L2953), change listener (~L5645). Verified: node merge = 254 rows identical to old baked values; `node --check` OK; local server serves both files (json MIME correct).
- Key evidence: `index.html:2013-2268` static array; `chartInstances` at `3210`; `initializeChart` at `3229`; `calcMetrics` at `2271`; ECharts CDN at `576`; selects at `592,594,638`; zero `fetch()`.
- `data.json`: 3 real keys × 254 rows. Missing-key tolerance required for `bseMidcap150Mom30`, `nse500Mom50`.
- Rules: No commits. No tests. Relative `fetch('./data.json')` only. Keep edits surgical.
